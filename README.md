# appwalk.dev

Landing page for [appwalk](https://github.com/kubstack/appwalk) — a CLI that turns AI browser exploration into verified Playwright tests.

Static HTML/CSS/JS, no build step. Deployed via Netlify.
